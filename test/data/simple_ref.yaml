swagger: '2.0'
info:
  version: 0.0.1
  title: Simple API with $ref.
paths:
  /:
    get:
      responses:
        200:
          description: OK
          schema:
            $ref: '#/definitions/Index'
definitions:
  Index:
    title: Index
    type: object
    properties:
      hello:
        type: string
        example: 'world'